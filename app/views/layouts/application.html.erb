<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title><%= content_for(:title) || t("ui.layout.app_name") %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main>
      <nav aria-label="<%= t("ui.layout.interface_language") %>">
        <span><%= t("ui.layout.interface_language_short") %>:</span>
        <%= link_to "PL", ui_language_switch_path(:pl) %>
        <%= link_to "EN", ui_language_switch_path(:en) %>
      </nav>

      <% if flash[:alert].present? %>
        <p><strong>Alert:</strong> <%= flash[:alert] %></p>
      <% end %>
      <% if flash[:notice].present? %>
        <p><strong>Notice:</strong> <%= flash[:notice] %></p>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
